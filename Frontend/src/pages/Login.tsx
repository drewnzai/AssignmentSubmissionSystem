import {Box} from "@mui/material";

export default function Login(){
    return(
        <Box display={"flex"}>
            <Box>
                <p>Login</p>
            </Box>
        </Box>
    );
}